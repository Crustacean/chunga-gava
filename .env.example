# docker-compose reads this file automatically (same directory as docker-compose.yml).
# Used for the Postgres/MinIO container defaults and to bake NEXT_PUBLIC_* vars into the
# frontend image at build time (see frontend/Dockerfile).

POSTGRES_USER=chungagava
POSTGRES_PASSWORD=chungagava
POSTGRES_DB=chungagava

MINIO_ROOT_USER=chungagava
MINIO_ROOT_PASSWORD=chungagava123

NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=
